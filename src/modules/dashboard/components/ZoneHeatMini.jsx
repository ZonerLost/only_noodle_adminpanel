// ZoneHeatMini removed — widget type "zones" has been removed from the dashboard.
// File left as a placeholder in case other modules reference it; import will return a no-op component.
import React from "react";

export default function ZoneHeatMini() {
  return null;
}
